

// ContactPage gives the Contact section its own address: /contact

import Contact from "../components/Contact.jsx";

function ContactPage() {
return (
    <main>
    <Contact />
    </main>
);
}

export default ContactPage;